---
title: Layouts
description: One page per page mode. The folder's meta.ts asks for an accordion directory, and this page turns off its previous and next links.
pagination: false
sidebar:
  label: Overview
---

Each page below sets a different `mode` in its frontmatter. The list comes from `directory: "accordion"` in `layouts/meta.ts`, and `pagination: false` leaves this page without previous and next links.
