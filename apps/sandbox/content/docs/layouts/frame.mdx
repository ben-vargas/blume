---
title: Frame
description: "`mode: frame` keeps the sidebar and nothing else of the page's chrome."
mode: frame
---

<div className="not-prose rounded-blume border border-border bg-muted/40 p-10">
  <p className="font-semibold text-2xl text-foreground">A framed page</p>
  <p className="mt-2 max-w-xl text-muted-foreground">
    `mode: frame` keeps the sidebar but leaves the rest to you: no title,
    description, breadcrumbs, table of contents, page-end links, or site footer.
    Use it for a custom tool or a dashboard that still sits in the docs'
    navigation.
  </p>
</div>
