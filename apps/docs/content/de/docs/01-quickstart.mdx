---
title: Schnellstart
description: >-
  Installiere Blume, erstelle ein neues Projekt und veröffentliche deine erste Dokumentationsseite in wenigen Minuten – und baue sie in deinem eigenen Tempo zu einer vollständigen Website aus.
sidebar:
  label: Schnellstart
  order: 1
---

Von einem leeren Ordner zu einer laufenden Doku-Website in wenigen Befehlen. Blume benötigt **Node.js 22.12 oder neuer** und einen `docs/`-Ordner mit mindestens einer `.md`- oder `.mdx`-Datei – mehr ist nicht einzurichten.

## Installieren und ausführen [#install-and-run]

<Steps>
  <Step title="Ein Projekt erstellen">
    `blume init` stellt ein paar Fragen – wo das Projekt erstellt werden soll, wie deine
    Doku-Website heißt, welches Template verwendet wird und wo deine Inhalte liegen – und
    erzeugt dann das Nötigste: einen Inhaltsordner, eine Konfigurationsdatei und eine
    `package.json` mit den Skripten `dev` und `build`. Die Abhängigkeiten installiert es
    mit dem Paketmanager, mit dem du es ausgeführt hast. Übergib `--yes`, um die Abfragen
    zu überspringen und die Standardwerte zu übernehmen, oder `--no-install`, um nur die
    Dateien zu schreiben.

    ```package-install
    npx blume init
    ```

    Unter pnpm 12 fügst du `--allow-build=esbuild` nach `pnpm dlx` hinzu (`pnpm dlx --allow-build=esbuild blume init`): pnpm 12 weigert sich, das Installationsskript von esbuild auszuführen, von dem Astro abhängt, solange du es nicht freigibst. Innerhalb eines bestehenden pnpm-Workspaces fügst du `allowBuilds: { esbuild: true }` zur `pnpm-workspace.yaml` des Workspaces hinzu; `init` erinnert dich daran. Mit Yarn 2 oder neuer schreibt `init` eine `.yarnrc.yml`, die Yarn auf eine `node_modules`-Installation umstellt, die Blume benötigt; unter Yarn 1 führst du `npx blume init` aus.

    ```txt
    docs/
      index.mdx
    blume.config.ts
    package.json
    ```

  </Step>
  <Step title="Den Entwicklungsserver starten">
    Im Projektverzeichnis generiert Blume die Laufzeitumgebung und stellt deine Doku
    mit Hot Reload bereit.

    ```package-install
    npm run dev
    ```

  </Step>
  <Step title="Für die Produktion bauen">
    Statisches HTML wird nach `dist/` geschrieben, mit einem darauf aufgebauten lokalen Suchindex.

    ```package-install
    npm run build
    ```

  </Step>
</Steps>

Du fügst Blume zu einem Projekt hinzu, das bereits eine `package.json` hat? `blume init` lässt bestehende Dateien unangetastet, also füge `"dev": "blume dev"` und `"build": "blume build"` zu dessen Skripten hinzu oder führe `npx blume dev` direkt aus.

:::tip
Blume funktioniert mit jedem Paketmanager und verlangt nie, dass du Astro oder Tailwind selbst einrichtest.
:::

## Schreibe deine erste Seite [#write-your-first-page]

Jede Seite ist Markdown oder MDX mit ein wenig Frontmatter – `title` und `description` werden automatisch als Seitenüberschrift und Einleitung gerendert. Füge dies in `docs/index.mdx` ein:

```mdx docs/index.mdx lineNumbers
---
title: Introduction
description: Welcome to my docs.
---

Welcome! Use **Markdown** and built-in components — no imports required:

:::note
Blume ships callouts, cards, tabs, steps, and more.
:::
```

Speichere sie, und der Entwicklungsserver lädt sofort neu. Navigation, Suche und Seiten-Metadaten werden aus deinen Dateien abgeleitet, sobald du sie hinzufügst – schreib einfach weiter, und die Website zieht mit.

## Nächste Schritte [#next-steps]

<CardGroup cols={2}>
  <Card title="Konfiguration" href="/docs/configuration" icon="file">
    Passe Titel, Theme, Suche und Deployment an.
  </Card>
  <Card title="Komponenten" href="/docs/content/components" icon="folder">
    Entdecke die integrierte Komponentenbibliothek.
  </Card>
  <Card title="Auffindbarkeit" href="/docs/discoverability" icon="lightbulb">
    Liefere `llms.txt`, Social Cards und einen MCP-Server aus.
  </Card>
  <Card title="Deployment" href="/docs/deployment" icon="rocket">
    Geh auf jedem statischen Host live.
  </Card>
</CardGroup>
