---
"blume": patch
---

The AI catalog's descriptions and sample queries read naturally for a site whose title already ends in "Docs": "get a page of the Acme Docs as Markdown" instead of "get a Acme Docs docs page as Markdown", and "Acme Docs API" instead of "Acme Docs docs API". An API reference's queries no longer say "API" twice ("what operations does the API Reference expose").
