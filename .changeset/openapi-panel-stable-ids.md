---
"blume": patch
---

API reference pages rebuild byte-for-byte: the "Try it" playground and the AsyncAPI message composer derive their error-message ids from the operation they render instead of drawing a random one on every build.
