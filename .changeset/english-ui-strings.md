---
"blume": patch
---

English pages (`en`, `en-GB`, and other `en-*` codes) on a site whose default locale isn't English now show Blume's built-in English interface strings, plus their own `i18n.ui` overrides, instead of the default locale's translation.
