---
"blume": patch
---

Changelog pages from a `githubReleases()` source are no longer copied to every other locale's URL, and no longer show the language switcher. Release notes publish in one language, so the copies repeated the same text at URLs missing from the sitemap and llms.txt.
