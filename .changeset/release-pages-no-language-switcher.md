---
"blume": patch
---

Changelog pages from a `githubReleases()` source no longer show the language switcher. Release notes exist in one language only, so every other locale just repeated the same text.
