---
"blume": patch
---

The `blume-migrate` skill now ships the oxfmt 0.67.0 directive patch in place of the 0.55.0 one, so a migrated project that formats with oxfmt also keeps titled fences like `:::warning[Heads up]` intact.
