---
"blume": patch
---

The Left and Right arrow keys follow the reading direction in `<Tabs>` and `<Tree>`: on a right-to-left page, Left moves to the next tab and expands a tree folder, and Right moves to the previous tab and collapses a folder.
