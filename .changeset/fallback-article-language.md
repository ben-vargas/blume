---
"blume": patch
---

An untranslated page served under another locale marks its article with the language it's written in (`lang="en"` on English text at `/ar/…`), beside the text direction it already set, so screen readers read it with the right voice instead of the page locale's.
