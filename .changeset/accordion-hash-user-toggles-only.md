---
"blume": patch
---

Only a reader opening an `<AccordionItem>` or `<Expandable>` writes its id to the URL hash. A `defaultOpen` item on the page a client-side navigation lands on no longer replaces the hash the reader arrived with.
