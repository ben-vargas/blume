---
"blume": patch
---

`blume-redirects.json` is written only for a static build with no host named, as documented. A static build for `vercel()`, `netlify()`, or `cloudflare()` gets that host's own redirect file alone.
