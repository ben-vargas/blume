---
"blume": patch
---

A `/changelog` tab now opens the changelog timeline instead of the newest entry, with no `href` needed, including under a `basePath`, where the generated index is still served at `/changelog`.
