---
"blume": patch
---

A header tab's `icon` now shows beside its label in the header, on a dropdown tab, and in the mobile navigation drawer. Before, `navigation.tabs[].icon` was accepted but never rendered.
