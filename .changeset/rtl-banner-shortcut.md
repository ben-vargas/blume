---
"blume": patch
---

In a right-to-left locale, the banner keeps its message's punctuation where it was written (an English sentence's closing period no longer jumps to its start), and the search button's shortcut reads "⌘K", not "K⌘", as does the search dialog's "⌘J" hint.
