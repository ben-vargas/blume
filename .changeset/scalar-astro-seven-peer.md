---
"blume": patch
---

Bump `@scalar/astro` to 0.4.21, which accepts Astro 7 as a peer, so a fresh project no longer installs with an "incorrect peer dependency" warning (or, under npm, a second nested copy of Astro).
