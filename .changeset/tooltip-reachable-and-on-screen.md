---
"blume": patch
---

A `<Tooltip>` stays open while the pointer moves from its trigger up to the panel, so a mouse can reach the panel's link. The panel now shifts to stay inside the viewport near either edge of the screen, and hidden tooltips no longer widen the page, which let a whole page scroll sideways on phones.
