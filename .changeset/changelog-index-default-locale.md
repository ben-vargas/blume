---
"blume": patch
---

The generated `/changelog` index links each release to its default-locale page. Under i18n every locale serves a page for an entry, so the index could link a release to whichever locale's route came last in the manifest — the Portuguese permalink on the English index.
