---
"blume": patch
---

The default 404 page now switches to the reader's locale under a `deployment.base` or `basePath` too, and its localized home link includes the `basePath`. `<Component />` preview frames now follow the OS color scheme when browser storage is blocked, instead of rendering without a theme.
