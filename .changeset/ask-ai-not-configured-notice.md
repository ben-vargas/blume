---
"blume": patch
---

When Ask AI's API key isn't set, the chat panel now shows the route's own notice — "Ask AI is not configured: set AI_GATEWAY_API_KEY." — instead of "Sorry, something went wrong." The notice names only the environment variable, never a value; any other error still shows the generic message.
