---
"blume": patch
---

When the API playground's request is blocked by CORS, its message now names the Blume 2 setting to fix it — `playground: { proxy: true }` on the `openapi()` reference — instead of the removed `openapi.playground.proxy` key.
